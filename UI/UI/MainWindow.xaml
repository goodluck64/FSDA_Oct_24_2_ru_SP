<Window x:Class="UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" ResizeMode="NoResize">
    <Grid ShowGridLines="True">
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <!-- Margin="10 20 30 40" -->
        <!-- Padding="5 10 15 20" -->
        <Button Grid.Column="1"
                Grid.Row="1"
                Content="Click me!"
                FontSize="16"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Padding="20 5"
                Click="Button_OnClick">
        </Button>

        <TextBlock x:Name="MyTextBlock" Grid.Row="0" Grid.Column="0" Text="C#" Foreground="DeepPink" FontSize="69"
                   Background="Blue">
        </TextBlock>
        <ListBox x:Name="MyItems" Grid.Column="1" Grid.Row="2" Grid.ColumnSpan="2" ItemsSource="{Binding Processes}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid ShowGridLines="True" HorizontalAlignment="Stretch">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding Id}" Foreground="DeepPink" Background="Aqua"></TextBlock>
                        <TextBlock Grid.Column="1" Text="{Binding Name}" Foreground="DeepPink" Background="Aqua"></TextBlock>
                        <TextBlock Grid.Column="2" Text="{Binding}" Foreground="DeepPink" Background="Aqua"></TextBlock>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>