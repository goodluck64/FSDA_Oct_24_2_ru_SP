<Window x:Class="SynchronizationEventsUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SynchronizationEventsUI"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid ShowGridLines="True">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2" Orientation="Horizontal">
            <Button Content="Start" Width="100" Height="25" Padding="0 3" Margin="0 0 5 0" Click="StartButton_OnClick"/>
            <Button Content="Pause" Width="100" Height="25" Padding="0 3" Margin="0 0 5 0" Click="PauseButton_OnClick"/>
            <Button Content="Stop" Width="100" Height="25" Padding="0 3" Margin="0 0 5 0" Click="StopButton_OnClick"/>
        </StackPanel>
        
        <ProgressBar x:Name="CounterProgressBar" Grid.Row="1" Grid.Column="2" VerticalAlignment="Center" Height="20" Margin="5"/>
        <TextBlock x:Name="CounterTextBlock" Grid.Row="1" Grid.Column="1" Text="0" HorizontalAlignment="Center" VerticalAlignment="Center" Background="DeepPink" Padding="5 0" Foreground="White"/>
    </Grid>
</Window>
